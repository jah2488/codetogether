<h2><%= @title %></h2>

<%= f = form_for @changeset, "#",
  id: "game-form",
  phx_target: @myself,
  phx_change: "validate",
  phx_submit: "save" %>

  <%= f %>
  <%= submit "Save", phx_disable_with: "Saving..." %>
</form>
